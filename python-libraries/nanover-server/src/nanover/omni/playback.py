from nanover.recording import PlaybackSimulation as PlaybackSimulation

import warnings

warnings.warn(
    "use `from nanover.recording import PlaybackSimulation`",
    DeprecationWarning,
    stacklevel=2,
)
