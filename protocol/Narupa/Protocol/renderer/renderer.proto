syntax = "proto3";

package narupa.protocol.renderer;

message RunGraphicsBenchmarkRequest {
  float duration = 1;
}

message RunGraphicsBenchmarkResponse {
  float averageFps = 1;
}

service RendererProvider {
  rpc RunGraphicsBenchmark(RunGraphicsBenchmarkRequest) returns (RunGraphicsBenchmarkResponse);
}
